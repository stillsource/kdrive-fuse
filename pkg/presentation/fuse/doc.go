// Package fuse wraps the kdrive use cases as a FUSE filesystem. It is the
// presentation layer of the kdrive-fuse module: kernel-driven node and handle
// state machines that route their I/O through the use cases.
package fuse
